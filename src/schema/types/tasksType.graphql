type Tasks {
    id: Int!
    text: String!
    userId: User!
  }

type Query {
    getTask(id: Int!): Tasks
    allTasks(userId: Int!): [Tasks]!
  }

  type Mutation {
    createTask(userId: Int!, text: String!): Tasks!
  }
