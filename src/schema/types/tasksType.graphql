type Tasks {
    id: Int!
    todo: String!
    userId: User!
  }

type Query {
    getTask(id: Int!): Tasks
    allTasks(userId: Int!): [Tasks]!
  }

  type Mutation {
    createTask(todo: String!): Tasks!
  }
