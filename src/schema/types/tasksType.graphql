type Tasks {
    id: Int!
    text: String!
    user: User!
  }

type Query {
    getTask(id: Int!): Tasks
    allTasks: [Tasks]!
  }

  type Mutation {
    createTask(username: String!, text: String!): Tasks!
  }
